name: Deploy to Production

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to deploy (only main or hotfix/*)'
        required: true
        type: string
        default: 'main'
      confirmation:
        description: 'Type "DEPLOY-TO-PRODUCTION" to confirm'
        required: true
        type: string

env:
  REGISTRY: localhost:5000
  IMAGE_NAME: goshortener
  ENVIRONMENT: production
  CLUSTER_NAME: goshortener-prd
  NAMESPACE: goshortener-prd
  REPLICAS: 3

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Validate Confirmation
        run: |
          if [ "${{ github.event.inputs.confirmation }}" != "DEPLOY-TO-PRODUCTION" ]; then
            echo "❌ Confirmação incorreta!"
            echo "Digite exatamente: DEPLOY-TO-PRODUCTION"
            exit 1
          fi
          echo "✅ Confirmação validada"
      
      - name: Validate Branch
        run: |
          BRANCH="${{ github.event.inputs.branch }}"
          
          if [[ "$BRANCH" != "main" ]] && [[ "$BRANCH" != hotfix/* ]]; then
            echo "❌ PRODUÇÃO só aceita branch 'main' ou 'hotfix/*'"
            echo "Branch fornecida: $BRANCH"
            exit 1
          fi
          
          echo "✅ Branch válida para produção: $BRANCH"

  build:
    needs: validate
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker Image
        run: |
          BRANCH="${{ github.event.inputs.branch }}"
          
          echo "🐳 Construindo imagem para PRODUÇÃO..."
          echo "📦 Branch: $BRANCH"
          echo "⚠️  ATENÇÃO: Deploy em ambiente de PRODUÇÃO!"
          
          docker build \
            --build-arg BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ') \
            --build-arg VCS_REF=${{ github.sha }} \
            --build-arg VERSION=$BRANCH \
            --build-arg ENVIRONMENT=production \
            -t ${{ env.IMAGE_NAME }}:prd-${{ github.sha }} \
            -t ${{ env.IMAGE_NAME }}:prd-latest \
            --load \
            .
          
          echo "✅ Imagem construída!"
      
      - name: Save Docker Image
        run: |
          echo "💾 Salvando imagem para transferência..."
          docker save ${{ env.IMAGE_NAME }}:prd-${{ github.sha }} | gzip > image.tar.gz
          
          echo "📊 Tamanho da imagem:"
          ls -lh image.tar.gz
      
      - name: Upload Image Artifact
        uses: actions/upload-artifact@v4
        with:
          name: docker-image-prd
          path: image.tar.gz
          retention-days: 7

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: production
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}
      
      - name: Download Image Artifact
        uses: actions/download-artifact@v4
        with:
          name: docker-image-prd
      
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          
          # Add host to known_hosts
          ssh-keyscan -p ${{ secrets.SSH_PORT || '22' }} ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
          
          echo "✅ SSH configurado!"
      
      - name: Transfer Docker Image
        run: |
          echo "📤 Transferindo imagem para servidor PRD..."
          
          scp -i ~/.ssh/deploy_key \
            -P ${{ secrets.SSH_PORT || '22' }} \
            image.tar.gz \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/tmp/goshortener-prd-image.tar.gz
          
          echo "✅ Imagem transferida!"
      
      - name: Deploy to Production
        run: |
          echo "🚀 Executando deploy em PRODUÇÃO..."
          echo "⚠️  ATENÇÃO: Deploy em ambiente de PRODUÇÃO!"
          
          ssh -i ~/.ssh/deploy_key \
            -p ${{ secrets.SSH_PORT || '22' }} \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            bash -s <<'EOF'
          
          set -e
          
          echo "📦 Carregando imagem Docker..."
          docker load < /tmp/goshortener-prd-image.tar.gz
          
          echo "🏷️  Tagueando imagem para registry local..."
          docker tag ${{ env.IMAGE_NAME }}:prd-${{ github.sha }} \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prd-${{ github.sha }}
          docker tag ${{ env.IMAGE_NAME }}:prd-${{ github.sha }} \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prd-latest
          
          echo "📤 Enviando para registry local..."
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prd-${{ github.sha }}
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prd-latest
          
          echo "☸️  Configurando kubectl para PRODUÇÃO..."
          kubectl config use-context kind-${{ env.CLUSTER_NAME }}
          
          echo "📝 Criando namespace se necessário..."
          kubectl create namespace ${{ env.NAMESPACE }} --dry-run=client -o yaml | kubectl apply -f -
          
          echo "🚀 Aplicando deployment..."
          
          # Verificar se deployment existe
          if kubectl get deployment goshortener -n ${{ env.NAMESPACE }} &>/dev/null; then
            echo "♻️  Atualizando deployment existente em PRODUÇÃO..."
            kubectl set image deployment/goshortener \
              goshortener=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prd-${{ github.sha }} \
              -n ${{ env.NAMESPACE }}
          else
            echo "🆕 Criando novo deployment em PRODUÇÃO..."
            kubectl create deployment goshortener \
              --image=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prd-${{ github.sha }} \
              --replicas=${{ env.REPLICAS }} \
              -n ${{ env.NAMESPACE }}
            
            # Adicionar labels
            kubectl label deployment goshortener \
              app=goshortener \
              version=v1 \
              environment=production \
              -n ${{ env.NAMESPACE }}
          fi
          
          echo "🔌 Configurando service..."
          kubectl expose deployment goshortener \
            --port=80 \
            --target-port=8080 \
            --type=ClusterIP \
            -n ${{ env.NAMESPACE }} \
            --dry-run=client -o yaml | kubectl apply -f -
          
          echo "⏳ Aguardando rollout..."
          kubectl rollout status deployment/goshortener -n ${{ env.NAMESPACE }} --timeout=600s
          
          echo "✅ Deploy PRODUÇÃO concluído!"
          
          echo "🧹 Limpando arquivo temporário..."
          rm -f /tmp/goshortener-prd-image.tar.gz
          
          EOF
      
      - name: Verify Deployment
        run: |
          echo "🔍 Verificando deployment em PRODUÇÃO..."
          
          ssh -i ~/.ssh/deploy_key \
            -p ${{ secrets.SSH_PORT || '22' }} \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            bash -s <<'EOF'
          
          kubectl config use-context kind-${{ env.CLUSTER_NAME }}
          
          echo ""
          echo "📊 Status dos Pods:"
          kubectl get pods -n ${{ env.NAMESPACE }} -l app=goshortener -o wide
          
          echo ""
          echo "🔌 Status do Service:"
          kubectl get svc -n ${{ env.NAMESPACE }} goshortener
          
          echo ""
          echo "📋 Deployment Info:"
          kubectl describe deployment goshortener -n ${{ env.NAMESPACE }} | grep -A 5 "^Replicas\|^Image"
          
          echo ""
          echo "🔍 Health Check dos Pods:"
          kubectl get pods -n ${{ env.NAMESPACE }} -l app=goshortener -o jsonpath='{range .items[*]}{.metadata.name}{"\t"}{.status.phase}{"\t"}{.status.conditions[?(@.type=="Ready")].status}{"\n"}{end}'
          
          EOF
      
      - name: Cleanup SSH
        if: always()
        run: |
          rm -f ~/.ssh/deploy_key
      
      - name: Deployment Summary
        run: |
          BRANCH="${{ github.event.inputs.branch }}"
          
          echo "╔══════════════════════════════════════════════════════════════╗"
          echo "║       DEPLOY PRODUCTION - SUCCESS                             ║"
          echo "╚══════════════════════════════════════════════════════════════╝"
          echo ""
          echo "🌐 Ambiente:       PRODUÇÃO"
          echo "📦 Cluster:        ${{ env.CLUSTER_NAME }}"
          echo "🏷️  Namespace:      ${{ env.NAMESPACE }}"
          echo "🌿 Branch:         $BRANCH"
          echo "🔖 Commit:         ${{ github.sha }}"
          echo "👤 Deployed by:    ${{ github.actor }}"
          echo "⏰ Deploy time:    $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          echo "🖥️  Server:         ${{ secrets.SSH_HOST }}"
          echo "🔢 Replicas:       ${{ env.REPLICAS }}"
          echo ""
          echo "✅ Aplicação deployada em PRODUÇÃO!"
          echo "⚠️  Monitorar logs e métricas!"
      
      - name: Notify Production Deploy
        run: |
          echo "🔔 NOTIFICAÇÃO: Deploy em produção realizado"
          echo "   Branch: ${{ github.event.inputs.branch }}"
          echo "   Commit: ${{ github.sha }}"
          echo "   By: ${{ github.actor }}"
